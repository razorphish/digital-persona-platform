{
  "name": "digital-persona-platform",
  "version": "1.0.0",
  "description": "Modern Digital Persona Platform with React, Next.js, tRPC, and Drizzle",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean",
    "db:push": "turbo run db:push",
    "db:migrate": "turbo run db:migrate",
    "db:studio": "turbo run db:studio"
  },
  "devDependencies": {
    "@turbo/gen": "^1.10.12",
    "turbo": "^2.5.6",
    "typescript": "^5.2.2"
  },
  "packageManager": "npm@10.2.4",
  "engines": {
    "node": ">=18.17.0"
  }
}
