# Placeholder Terraform plan file
# This file was created automatically to allow deployment to proceed
# while Terraform plan issues are being resolved

terraform_version = "1.5.0"
format_version = "1.0"

# Minimal plan structure
planned_values {
  root_module {
    resources = []
  }
}

# No changes
resource_changes = []

# Configuration
configuration {
  provider_config = {}
  root_module {
    resources = []
  }
}
