# DPP Platform Terraform Backend Configuration
# This ensures DPP uses a unique state file key for proper isolation

bucket = "hibiji-terraform-state"
key    = "dev/dev01-dpp/terraform.tfstate"
region = "us-west-1"
encrypt = true
dynamodb_table = "hibiji-terraform-locks"

