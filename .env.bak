# Local Development Environment Variables
# Created to match Docker Compose development setup

# Application Environment
NODE_ENV=development
PORT=4001

# Database Configuration (matches docker-compose.dev.yml PostgreSQL)
DATABASE_URL=postgresql://dpp_user:dpp_password@localhost:5432/digital_persona

# JWT Configuration
JWT_SECRET=development-jwt-secret-change-in-production

# CORS Configuration (matches your local ports)
CORS_ORIGIN=http://localhost:4000,http://localhost:3100

# AWS/S3 Configuration (for local development)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin123
S3_BUCKET=dev-dev01-dpp-uploads

# Auth Configuration
BETTER_AUTH_SECRET=development-auth-secret-change-in-production
BETTER_AUTH_URL=http://localhost:4001/api/auth

# Redis Configuration (matches docker-compose.dev.yml)
REDIS_URL=redis://localhost:6379

# Frontend URLs
NEXT_PUBLIC_API_URL=http://localhost:4001
NEXT_PUBLIC_WEBSITE_URL=http://localhost:4000
NEXT_PUBLIC_APP_ENV=development
